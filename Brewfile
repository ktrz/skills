brew "git"
brew "gh"
brew "yq"
brew "pre-commit"
# node runs the vendored handover-doc validator (_shared/handover-validator).
# The committed bundle is self-contained, so only the runtime is needed.
brew "node"
