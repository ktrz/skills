{
  "target": "demo",
  "stable_path": "tests/evals/fixtures/good-skill",
  "description": "Test-only scenario set whose stable_path points at the good fixture — drives the runner's exit-0 path end-to-end.",
  "scenarios": [
    {
      "id": "demo-contract",
      "kind": "invariant",
      "checks": [
        { "type": "description_contains", "value": "open a pull request", "desc": "trigger phrase kept" },
        { "type": "section_present", "value": "Create the PR", "desc": "PR-creation section kept" },
        { "type": "body_contains", "value": "Do NOT push the branch yourself", "desc": "push invariant kept" }
      ]
    }
  ]
}
