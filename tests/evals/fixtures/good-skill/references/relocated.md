# Relocated reference material

This file exists so the harness tests can prove that `loadSkill` includes a
variant's `references/*.md` in the checkable body — a progressive-disclosure
rewrite that moves spec text out of SKILL.md must not read as drift.

## Relocated spec section

TEMPLATE_MARKER_IN_REFERENCE
